
<h1>Products</h1>
<div class="container mx-auto mt-4">
  <div class="row">
    <% @products.each do |product| %>
      <%= render product %>
    <% end %>
  </div>
  <% if @products.length == 0 %>
    <p>No existen productos en stock.</p>
  <% end %>
</div>
